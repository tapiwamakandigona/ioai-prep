# Teacher guide content — one entry per slide, written in the simplest English possible.
# mean = "what this actually means" paragraphs; say = what to say out loud; ask = likely questions.

GUIDE = [
dict(n=1, title="Cover — How machines learn to see",
mean=["This is just the title page. The talk is about computer vision: how a computer can look at a photo and tell you what is in it.",
"One promise to make at the start: every number in this deck is real. We really ran these experiments on a normal laptop. Nothing is made up."],
say="Today you will learn how a computer recognizes a cat, finds it in a photo, and even draws one. The only math you need is multiplying small numbers."),

dict(n=2, title="The plan",
mean=["The day has five parts, and they build on each other like a story:",
"Part 1: the one basic trick a computer uses to 'see' (we will do it by hand with real numbers). Part 2: how the computer learns that trick by itself, by practicing. Part 3: a shortcut — instead of teaching a computer from zero, you borrow one that someone already taught. Part 4: the cool extras — finding objects in a photo, cutting them out, talking to a model in English, and creating new images. Part 5: cheat sheets to keep.",
"The rule for the whole day: we never use a fancy word before explaining the idea behind it in plain words first."],
say="One day, one story. If you can multiply small numbers, you can follow every single step."),

dict(n=3, title="Five starter words",
mean=["Before starting, we agree on five words. These are the only words used without warning later, so make sure YOU are comfortable with all five:",
"MODEL = the 'machine' itself. It is really just a huge collection of numbers. You put something in (a photo), it gives a guess back ('cat').",
"DATASET = the pile of examples we teach with. For us: lots of photos, each with its correct answer attached.",
"LABEL = the correct answer for one example. The photo of a cat has the label 'cat'.",
"TRAINING = showing the model examples again and again until its guesses become good. Like practice.",
"ACCURACY = a score. Out of 100 tries, how many did it get right? 90% accuracy = 90 right out of 100."],
say="Read all five out loud, slowly. Tell them: every other word today gets explained before we use it.",
ask=[("Is a model a robot?", "No — it is just a very long list of numbers stored in a file, plus a recipe for using them. No body, no eyes, no brain.")]),

dict(n=4, title="(Dark slide) A computer has never seen a cat",
mean=["This is a dramatic pause slide. The point: a computer has no eyes. It has never 'seen' anything. So when we say it recognizes a cat, something else must be going on — and that something is just math on numbers.",
"This question ('so what DOES it get?') sets up the whole next slide."],
say="Pause. Ask the room: if it has no eyes, what does the computer actually get when we give it a photo? Let them guess before advancing."),

dict(n=5, title="A photo is just a grid of numbers",
mean=["This is the first big idea. If you zoom very far into any photo, it breaks into tiny squares. Each square is one PIXEL.",
"Each pixel holds ONE number between 0 and 255. 0 means black, 255 means white, numbers in between are shades of gray. So a black-and-white photo is literally a big grid (table) of numbers, like a spreadsheet.",
"A color photo is THREE of these grids stacked on top of each other: one grid says how red each pixel is, one how green, one how blue. Mixing red, green and blue makes every color.",
"The grid of numbers on the slide is REAL — it is the actual numbers inside the small square marked on the cat's eye.",
"The scary word TENSOR just means 'a grid of numbers, possibly several stacked'. That is all. A photo is a tensor."],
say="Ask: where are the BIG numbers — in the bright fur or the dark pupil? (Bright fur, because 255 = white.) Then the key sentence: three grids of numbers — that is ALL the computer ever gets.",
ask=[("Why 0 to 255?", "Computers store numbers in bytes. One byte can hold exactly 256 different values (0–255). It is just a convention.")]),

dict(n=6, title="Convolution by hand — THE most important slide",
mean=["Take your time here. If the students understand this slide, the rest of the day is easy.",
"The tiny image: a column of 2s (bright stripe) next to 0s (dark area). Where bright meets dark, there is an EDGE — a border. Question: can math FIND that edge?",
"The stencil: nine numbers we chose — left column is 1s (plus), right column is −1s (minus), middle is 0s.",
"The move: lay the stencil on the image. Multiply each image number by the stencil number sitting on top of it. Add all nine answers. That single total is the result for that position.",
"On the edge: the 2s sit on the +1 side, the 0s on the −1 side → total = 6, a big number. The math is shouting 'my pattern is HERE!'",
"On a flat dark area: everything is 0 → total = 0. The math says 'nothing here.'",
"So a stencil of nine numbers can DETECT an edge. This multiply-and-add move is called CONVOLUTION, and the stencil is called a FILTER (or KERNEL). Those two words sound scary but you just did the whole thing with times tables."],
say="Do the nine multiplications OUT LOUD with the room before showing the total. Then the reveal: congratulations, you just did a convolution.",
ask=[("Who chose the nine stencil numbers?", "We did, for teaching. In a real network nobody chooses them — they are learned. That is Part 2.")]),

dict(n=7, title="Slide it everywhere — feature maps",
mean=["One position gives one number. Now slide the same stencil across the WHOLE photo, stopping at every position, doing the multiply-and-add each time, and writing down each answer.",
"All those answers together form a new image. It is bright wherever the stencil's pattern was found, dark where it was not. This 'glow map' is called a FEATURE MAP.",
"On the slide you can see it really works: with our vertical-edge stencil, the cat's whiskers and eye outlines glow, because those are vertical-ish edges. Rotate the stencil 90 degrees and horizontal edges glow instead.",
"Key idea: the NUMBERS in the stencil decide WHAT pattern is found. Different numbers = different pattern."],
say="Point at the glowing whiskers: this is the real output of the exact stencil we just used, run on this exact photo."),

dict(n=8, title="Stride and padding",
mean=["Two small, boring-but-needed rules about HOW the stencil walks across the photo. Don't overthink these.",
"STRIDE = the step size. Stride 1: the stencil stops at every position. Stride 2: it jumps two cells each move, so it makes half as many stops, and the answer map comes out half the size. Faster, but a blurrier view.",
"PADDING = a ring of zeros glued around the photo's border. Why? Two problems: (1) without it, the border pixels never get to sit under the CENTER of the stencil, so the edges of the photo get ignored; (2) the answer map comes out a bit smaller than the photo every time, so after many layers it shrinks away. The ring of zeros fixes both. The zeros are 'invisible' — they add nothing to the multiply-and-add."],
say="Both are just walking rules: stride = how big the steps are, padding = zeros around the border so the edges are not ignored. Don't linger here."),

dict(n=9, title="Pooling — shrink the map, forgive small shifts",
mean=["Take the glow map and cut it into little 2×2 windows. From each window, keep ONLY the biggest number and throw the other three away. This is MAX POOLING.",
"Why keep the max? The big number means 'the pattern was found here'. We only care IF it was found in this small area — not its exact pixel. So we keep the strongest signal per window.",
"Two wins: (1) the map becomes half the size, so everything after is 4× cheaper to compute; (2) if the pattern shifts by one pixel, the biggest number in the window is usually still the same — so tiny shifts of the object stop mattering. A cat slightly to the left is still a cat.",
"Check the example with them: top-left window holds 1, 3, 4, 8 → keep 8.",
"Cousins: AVERAGE POOLING keeps the mean instead of the max. And at the very end of a network, GLOBAL AVERAGE POOLING averages each whole map down to ONE number — making a short summary list of the photo."],
say="Keep each window's winner. Verify one quadrant together out loud."),

dict(n=10, title="ReLU — the 'keep the good news' switch",
mean=["The rule itself is tiny: go through the map and replace every NEGATIVE number with 0. Positive numbers pass through unchanged. That is literally all ReLU does.",
"The hard part is WHY. Here is the simplest version: convolution is just multiplying and adding — 'straight-line math'. If you stack many straight-line steps, the result is still one straight-line step. Like stacking rulers: a stack of rulers is still a ruler. So 50 layers would compute nothing more than 1 layer could. Depth would be fake.",
"Putting one little 'bend' (the ReLU) between layers breaks the straight line. Now each layer can build something genuinely NEW on top of the previous one. The bend is what makes deep networks actually deep.",
"Words: any 'bend' function between layers is called an ACTIVATION FUNCTION. ReLU is the standard modern one. SIGMOID and TANH are older bends — students only need to recognize the names."],
say="Anchor sentence: negatives become zero — and without that little bend, a 50-layer network is exactly as smart as a 1-layer one.",
ask=[("Why would the map have negative numbers at all?", "Stencils contain negative numbers (like our −1 column), so totals can come out negative. Negative roughly means 'the opposite of my pattern is here' — ReLU throws that away and keeps only positive evidence.")]),

dict(n=11, title="The complete CNN stack",
mean=["Now plug everything together into one pipeline. Photo goes in → a layer of many stencils + ReLU → pooling → another layer of stencils + ReLU → pooling → global average → final scores ('cat 92%').",
"The magic of stacking: layer 1's stencils look at the photo and find edges. Layer 2's stencils look at the GLOW MAPS of layer 1 — so they find combinations of edges: corners, textures (fur, stripes). Layer 3 finds combinations of textures: an ear, an eye, a wheel. The ladder goes EDGES → TEXTURES → PARTS → OBJECTS. Nobody tells the network to do this — it appears by itself.",
"Words on this slide: the whole stack is a CNN (convolutional neural network). The short summary list of numbers near the end is the EMBEDDING. SOFTMAX is the small last step that turns scores into percentages that add to 100. The whole job (one label per photo) is called IMAGE CLASSIFICATION."],
say="Stencils on stencils. Layer 1 finds edges, layer 2 finds textures, layer 3 finds parts. Nobody told it to — that ladder appears on its own."),

dict(n=12, title="Code card 1 — the CNN in PyTorch",
mean=["Don't fear this slide — every line is a thing the students already understand. Read the COMMENTS, not the syntax.",
"Conv2d(3, 32, kernel_size=3) = 'make 32 stencils, each 3×3, that look at the 3 color grids'. ReLU() = the bend. MaxPool2d(2) = pooling. AdaptiveAvgPool2d(1) = global average (the summary list). Linear(64, 10) = the final scoring step that turns the summary into 10 class scores.",
"This is a real, complete classifier — this exact shape scored about 50% on a 10-class photo task, where blind guessing scores 10%.",
"The knob box is exam material: more stencils = more patterns spotted but slower; bigger stencils = sees bigger patches but many more numbers to learn; DELETE the ReLU lines and accuracy collapses (the ruler-stack problem); more layers = deeper ladder but needs more data and time."],
say="Read the comments out loud, mapping each line to the idea they already know. Then quiz them gently: what happens if I delete the ReLU lines?"),

dict(n=13, title="Quick check 1",
mean=["First checkpoint. Run it relaxed and out loud — popcorn style, no grades. The answers are on the slide.",
"The four questions test: (1) the computer only gets grids of numbers; (2) a filter slides, multiplies and adds, and lights up where its pattern is; (3) ReLU stops the stack collapsing into one layer; (4) pooling makes maps smaller and forgives small shifts.",
"The myth box matters: AI does NOT see like us. It has no eyes and no idea what a cat 'is'. It does millions of multiply-and-adds on a number grid."],
say="If the room struggles on the ReLU question, re-anchor with: 'the bend that makes depth real — stacked rulers are still a ruler.'"),

dict(n=14, title="(Dark slide) Nobody programs the filters",
mean=["Transition into Part 2. On slide 6, WE chose the nine stencil numbers by hand. A real network has MILLIONS of stencil numbers. Nobody could choose them all by hand — and nobody does.",
"They start as random noise, and the network finds good values by practicing. How that practice works is the whole of Part 2."],
say="One breath: 'we picked nine numbers; a real network has millions, and nobody picks them. They are learned. How?' Advance."),

dict(n=15, title="The learning loop (loss, backpropagation, epoch)",
mean=["Learning is the hot-and-cold game. The loop: (1) show the network a photo. Its stencils are random, so it guesses badly: '70% dog'. (2) We know the label is 'cat', so we calculate ONE number that says how wrong the guess was. That number is the LOSS. Big loss = ice cold. Small loss = warm. That's the entire meaning of the word 'loss': the wrongness score.",
"(3) A mathematical recipe then works out, for EVERY stencil number in the network, which direction to nudge it (up or down a tiny bit) to make the same mistake slightly smaller. That recipe is called BACKPROPAGATION — you don't need to know its inside, just its job: 'it tells every number which way to nudge'.",
"(4) Repeat with the next photo. One photo = one tiny nudge. After a million photos, the random stencils have turned into the edge detectors from slide 7. Nobody put them there.",
"An EPOCH = one full pass through the whole dataset (every photo seen once)."],
say="Guess → compare with the answer → nudge every number a tiny bit → repeat. That is all training is."),

dict(n=16, title="Gradient descent and learning rate",
mean=["A picture for the nudging. Imagine every possible setting of all the stencil numbers as one point in a landscape with hills and valleys. The HEIGHT of each point = the loss (the wrongness) you'd get with that setting. Good settings live in the valleys.",
"Training = placing a ball somewhere random and rolling it DOWNHILL, step by step. That procedure is GRADIENT DESCENT ('gradient' just means slope — follow the slope down).",
"The LEARNING RATE is the SIZE of each step. It is the single most important setting in training. Too small: the ball crawls — training takes forever. Too big (right picture): the ball leaps right over the valley, bounces back and forth across it forever, and never settles. The loss 'explodes'.",
"A safe default the students can quote: the Adam optimizer with learning rate 0.001, shrinking the rate as training goes."],
say="Wrongness is a landscape, training rolls a ball downhill, and the learning rate is how big each step is.",
ask=[("How does the ball know which way is downhill?", "Calculus gives the slope at the current point for every number at once. In code it is one line: loss.backward(). You never compute it by hand.")]),

dict(n=17, title="Overfitting — memorizing is not understanding",
mean=["Picture a student who memorizes last year's exam papers. Perfect on the practice papers — fails the real exam, because the real exam has new questions. Networks do exactly this: with enough practice on the same photos, they start memorizing those exact photos instead of learning what a cat looks like. That is OVERFITTING.",
"The protection: split the dataset in two. TRAIN photos = study material. TEST photos = the real exam — the network NEVER trains on these, we only use them to measure honestly.",
"The graph: train accuracy keeps climbing forever (memorizing always helps the study set). Test accuracy rises, peaks, then FALLS. The moment it falls is the moment memorizing started. So: stop at the peak.",
"Names to recognize (not to deeply learn): EARLY STOPPING (stop at the peak), DROPOUT and WEIGHT DECAY (built-in 'forget a little on purpose' brakes), and getting more varied data — the best fix of all."],
say="Never grade a model on its study material. Watch the test score, and stop when it peaks."),

dict(n=18, title="Augmentation — free extra photos",
mean=["A flipped photo of a cat is still a cat. A slightly cropped one too. So: every time a training photo is used, change it a little at random — flip it, shift the framing, tweak the colors. The network can never memorize an exact photo because it never sees the same photo twice. It is forced to learn the IDEA of the cat instead. This is AUGMENTATION — basically free extra training data.",
"The code lines on the slide are the real ones from our own training run.",
"Two traps to mention: (1) NEVER augment the test photos — the exam must stay fixed. (2) Every change must keep the label true: flipping a digit 6 turns it into a 9 — that augmentation would destroy the label."],
say="A flipped cat is still a cat — so flip, crop and recolor on the fly, and memorizing becomes impossible."),

dict(n=19, title="What the network taught itself",
mean=["This slide is proof that the whole story is true. You are looking at the REAL first-layer stencils of ResNet18, a famous network trained on 1.28 million photos.",
"Every one of these little patches started as pure random noise. After training, they became edge detectors and color detectors — strikingly similar to the stencil we built BY HAND on slide 6.",
"Nobody drew them. The network discovered, by rolling downhill on the loss, that edges are the right place to start seeing. Layer 2 (not shown) finds textures like fur and stripes; higher layers find ears, eyes, wheels."],
say="Nobody drew these. They started as random noise — and training turned them into the same kind of stencil we built by hand."),

dict(n=20, title="RECEIPTS — the three numbers to remember",
mean=["The most quotable slide of the deck. The task: classify small photos into 10 classes (cat, ship, truck...). Blind guessing scores 10%. Everything ran on a normal laptop CPU, and is reproducible from our repo.",
"Bar 1: a small CNN trained from scratch on 4,000 photos → 50.0%. Bar 2: a borrowed (pretrained) ResNet18, gently retrained on only 1,000 photos → 74.2%. Bar 3: CLIP, with ZERO training photos → 88.5%.",
"Sit with that: fewer photos, better score — and then NO photos, best score. The rest of the talk explains those two bigger bars: Part 3 = borrowing trained eyes, Part 4 = models that learned from the whole internet."],
say="Before revealing, ask the room to guess the from-scratch score — closest wins. Then: remember these three numbers — 50, 74, 88.5 — with 4,000, 1,000, and ZERO photos."),

dict(n=21, title="(Dark slide) Never start from scratch",
mean=["Transition into Part 3. Someone already spent weeks of computer time training a network to see, on 1.28 million photos. Those 'trained eyes' are free to download. So in practice, you almost never train from zero."],
say="One breath, advance."),

dict(n=22, title="Transfer learning — borrow trained eyes",
mean=["The chef story: a chef who switches from Italian to Japanese cooking does not relearn how to chop and season — only the new recipes. Networks transfer the same way.",
"A network trained on ImageNet (1.28 million photos, 1000 classes) already knows edges, textures, fur, wheels — skills useful for ANY photo task. So: keep all the filter layers ('the trained eyes'), chop off only the final decision layer, bolt on a fresh one for YOUR classes, and train mostly just that new piece.",
"That is exactly how 1,000 photos beat 4,000 on the receipts slide: 74.2% vs 50.0%. The eyes came pre-trained.",
"Words: the downloaded network = PRETRAINED MODEL or BACKBONE. Reusing it = TRANSFER LEARNING. The gentle retraining = FINETUNING. Locking layers so training can't change them = FREEZING.",
"Rule of thumb worth quoting: with a pretrained start, 100–1,000 photos per class is often enough."],
say="Keep the trained eyes, swap the final decision layer. That is how 1,000 photos beat 4,000."),

dict(n=23, title="ResNet — why deeper suddenly worked",
mean=["A real story from 2015: researchers found a 56-layer network scored WORSE than a 20-layer one. That should be impossible — a deeper net could in principle just copy the shallow one and ignore the extra layers. Something was broken.",
"What was broken: during training, the 'nudge' signal must travel backwards through every layer. Like a message whispered down a line of 50 people, it fades to almost nothing by the time it reaches the early layers. Deep nets couldn't train.",
"The fix is one humble arrow: a SKIP CONNECTION (or residual connection) — an express lane around every block of layers. The input skips ahead and is simply ADDED back to the block's output. So output = input + the block's learned CHANGE.",
"Two consequences: each block only needs to learn the small change it wants to make (and 'do nothing' becomes easy: change = 0); and the learning signal can ride the express lanes backwards without fading. Suddenly 50, 100, even 152 layers train fine.",
"RESNET-18 and RESNET-50 are the standard pretrained backbones; the number counts the layers."],
say="Tell the 2015 surprise first, then the fix: one express lane that adds the input back. Each block learns only the small change."),

dict(n=24, title="Code card 2 — transfer learning in four lines",
mean=["The exact recipe behind the 74.2% bar. Line 1: download ResNet18 with its ImageNet-trained weights ('borrow trained eyes'). Lines 2–3: freeze every parameter so training can't damage the eyes. Line 4: replace the final decision layer (fc) with a fresh one for our 10 classes. Then train normally — only the new layer actually learns.",
"Knob box (exam material): unfreeze everything = more flexible but easily overfits with few photos, and a high learning rate destroys the pretrained eyes. Under ~500 photos: keep frozen — frozen usually wins. Plenty of photos: unfreeze, but with a tiny learning rate (0.0001) for the backbone. weights=None = random start = you're back to the 50% bar."],
say="Four honest lines: borrow, freeze, swap, train."),

dict(n=25, title="(Dark slide) One label per photo was just the beginning",
mean=["Transition into Part 4. So far the machine answers one question: 'what is in this photo?' — one label per photo. The same trained eyes, with different 'heads' bolted on top, can do much more: find every object, cut them out pixel by pixel, understand English descriptions, and create brand-new images."],
say="One breath, advance."),

dict(n=26, title="Object detection — WHERE, not just what",
mean=["Detection answers 'what is WHERE'. The model outputs a LIST: for every object it finds, a rectangle around it (BOUNDING BOX), a label, and a CONFIDENCE (how sure it is). All found in one pass over the photo. The slide shows real output on our cat photo: 'cat 97%'.",
"Messy detail worth knowing: the raw model actually proposes MANY overlapping boxes around the same object. A cleanup step called NMS (non-maximum suppression) keeps the most confident box of each cluster and deletes the duplicates.",
"How do we grade a predicted box? IoU (intersection over union): area where the predicted and true boxes OVERLAP, divided by the area they COVER TOGETHER. 1.0 = perfect match; 0.5 or more traditionally counts as a hit.",
"Knobs: raise the confidence cutoff → fewer false alarms but real objects get missed. Make NMS stricter → two cats sitting close together may merge into one box."],
say="Box + label + confidence, for every object, in one pass. IoU grades a box, NMS deletes the duplicates."),

dict(n=27, title="Three detector families — YOLO, SSD, DETR",
mean=["Students only need the names, the one-line idea, and the 'character' of each. All three reuse a pretrained backbone as their eyes.",
"YOLO ('You Only Look Once'): cut the photo into a grid; every grid cell predicts boxes for objects centered in it — one single pass. The speed king: 150+ photos per second on a GPU. Pick for live video, robots, drones.",
"SSD: same one-pass idea, but predicts from several map sizes at once, so big maps catch small objects. Fast and light — this is the model that produced our cat slide. Pick for phones and small devices.",
"DETR: a transformer head asks about 100 learned 'questions' ('is there an object like X anywhere?'). No grid, and no NMS cleanup needed. Elegant and accurate, slower to train. Pick when accuracy beats speed.",
"For the exam: know the names, the one-pass idea, and that YOLO = real-time. Internals are NOT needed."],
say="Know the names and characters: YOLO = speed king, SSD = light for phones, DETR = the elegant transformer one."),

dict(n=28, title="Segmentation — a label for every pixel",
mean=["Boxes are rectangles, but cats are not. SEGMENTATION answers, for every single pixel: which class does this pixel belong to? The answer sheet — a grid where each pixel is marked 'cat' or 'not cat' — is the SEGMENTATION MASK. Multiply photo × mask and you get a perfect cutout.",
"Two flavors: SEMANTIC = 'all cat pixels' as one group; INSTANCE = 'cat #1's pixels vs cat #2's pixels' separately.",
"The classic architecture is shaped like the letter U (U-NET): the left side SHRINKS the photo (convolutions + pooling — exactly Part 1) to understand WHAT is there; the right side GROWS it back to full size to say WHERE, pixel-perfect.",
"Problem: shrinking loses fine detail. Fix: express lanes copy the crisp edges from the shrinking side straight across to the growing side. Skip connections again — same trick as ResNet!",
"Where they've met it: background blur in video calls, medical scans, self-driving cars."],
say="A class for EVERY pixel. Shrink to understand what, grow back to say where — and skip connections carry the sharp edges across."),

dict(n=29, title="Quick check 2",
mean=["Checkpoint for Parts 3 and 4a. Answers on the slide: (1) 1,000 beat 4,000 because of pretrained eyes; (2) a skip connection lets a block learn only the small CHANGE, and lets the learning signal travel deep without fading; (3) detection = box + label + confidence per object, segmentation = a class per pixel; (4) NMS throws away duplicate overlapping boxes.",
"The myth box is the most important: 'you need millions of photos and a giant computer' — FALSE. With a pretrained backbone, 100–1,000 photos per class and a laptop is often enough. Our 74.2% ran on a CPU."],
say="Run it popcorn-style. Spend the most time on the myth — it is the most empowering fact of the day."),

dict(n=30, title="Self-supervised learning — no labels at all",
mean=["The expensive part of a dataset is not the photos — it's the LABELS, because humans must tag every photo by hand. The internet has billions of photos with no tags. Can a network learn to see from unlabeled photos? Yes — trick: make the photo its own label.",
"The recipe: take one photo. Make TWO different augmented views of it (crop one; flip and blur the other — slide 18's tricks reused). Rule for the network: embeddings of views of the SAME photo must land CLOSE together; embeddings of DIFFERENT photos must land far apart. Pull together, push apart — that's why it's called CONTRASTIVE LEARNING.",
"To satisfy this rule, the network is forced to learn what actually MATTERS in an image (the cat-ness survives a crop and a blur; the exact pixels don't). Real seeing skills, zero human labels. This whole family is SELF-SUPERVISED LEARNING — the data supplies its own training signal.",
"Afterwards, add a small decision layer trained with just a few labeled photos — nearly-free trained eyes."],
say="Two augmented views of the same photo must land close together. To manage that, the network must understand images — and no human ever labeled anything."),

dict(n=31, title="CLIP — the model you can talk to",
mean=["CLIP takes the contrastive trick one step further: don't teach it classes at all. Describe the classes in English.",
"CLIP is TWO encoders: one turns PHOTOS into embeddings, the other turns SENTENCES into embeddings — into the SAME space. Trained on 400 million (photo, caption) pairs from the internet, with the pull-together/push-apart rule: each photo and its own caption pull close.",
"To classify any photo: embed the photo, embed each candidate sentence ('a photo of a cat', 'a photo of a dog', ...), pick the CLOSEST sentence. Want new classes? Just write new sentences. ZERO retraining. That is ZERO-SHOT classification — solving a task with no task-specific training examples — and it is the 88.5% bar from the receipts slide.",
"The sentence you classify with is called the PROMPT, and its wording matters: 'a photo of a cat' scores a few % better than just 'cat', because that's what internet captions looked like.",
"Real output on our cat photo: cat 97.9%, tiger 1.6%, dog 0.5%, pizza 0.0%."],
say="Two encoders, one shared space for photos and sentences. To classify: pick the closest sentence. New classes = new sentences."),

dict(n=32, title="Code card 3 — zero-shot with CLIP",
mean=["Walk this one slowly — it is the complete zero-shot recipe, honest, nothing hidden. Load CLIP (photo eyes + text eyes). Write the labels as sentences. Turn the photo into a tensor, the sentences into tokens. Encode BOTH into the same space. Measure closeness, turn into percentages with softmax (slide 11's word!).",
"Knobs: swap the labels list → an entirely new classifier, instantly. Phrase labels as 'a photo of a ...' → accuracy up for free. Ask something weird → 'a photo of a grumpy cat' works too; it is just a sentence."],
say="Nine lines, no training loop anywhere — and it scored 88.5%. Output on our cat: 97.9%."),

dict(n=33, title="GANs — a counterfeiter and a detective",
mean=["Now the OTHER direction: not understanding images, but CREATING them. A GAN is two networks training against each other.",
"The GENERATOR (counterfeiter) turns random noise into fake photos. The DISCRIMINATOR (detective) is shown real photos and fakes, and must call real-or-fake. Every round the detective gets sharper, so the counterfeiter must get better — an arms race that ends in photorealistic fakes.",
"The honest subtlety students love: the counterfeiter NEVER sees a real photo. It learns only from the detective's feedback ('caught you'). It learns to paint faces without ever seeing one — only from being caught.",
"GANs are famous and fast, but moody to train. Classic failure: MODE COLLAPSE — the counterfeiter finds ONE face that always fools the detective and prints it forever (the one-trick forger)."],
say="A counterfeiter and a detective, raising each other's game — and the counterfeiter never sees a real photo."),

dict(n=34, title="Diffusion — how modern image generators work",
mean=["DALL·E, Stable Diffusion, Midjourney — under the hood, this. And the students already know every ingredient.",
"Training: take a real photo, add a random amount of noise (static), and ask a network to predict WHAT noise was added. That's it. Easy, stable homework — no arms race, no moody training.",
"Generating: start from PURE noise and apply the network's only skill — 'remove a little noise' — about 50 times in a row. Step by step an image emerges from the static. Read the slide's photo strip RIGHT to LEFT: pure noise → cat.",
"A text prompt steers every denoising step (using CLIP-style text understanding — last slide's idea reused!). That's how 'a cat astronaut on the moon' comes out.",
"Knobs: GUIDANCE SCALE = how hard the prompt steers (~7.5 typical; too high = burnt, oversaturated images). STEPS = more is better but slower; 20–50 is common.",
"GANs vs diffusion: diffusion won the image-quality war (stable training, more variety); GANs are still faster at generating."],
say="Its only skill is removing a bit of noise. Generation = that skill 50 times, starting from pure static, with the prompt steering each step."),

dict(n=35, title="Quick check 3",
mean=["Final checkpoint, Part 4b. Answers on the slide: (1) CLIP classifies unseen things by embedding photo and sentences into the same space and picking the closest sentence; (2) in a GAN, the GENERATOR never sees a real photo; (3) a diffusion model's only skill is removing a little noise — applied ~50 times from pure noise; (4) in self-supervised learning, the labels are replaced by the photo itself: two augmented views must land close together."],
say="Popcorn-style again. If these land, the syllabus is covered."),

dict(n=36, title="Cheat sheet 1 — the syllabus map",
mean=["Every IOAI Section 3 topic, the slide where it was covered, and a one-line summary to remember. This is the revision index for the whole deck.",
"Tell the students to screenshot this slide — before the exam they can check each row and jump back to the right slide if anything feels shaky."],
say="This is your revision index. Screenshot it."),

dict(n=37, title="Cheat sheet 2 — all 30 terms",
mean=["Every word 'earned' during the day, each with a one-line plain-words meaning. All 30 of them.",
"Useful for you too: if you can read every line of this slide and nod, you understand the deck. Any line that makes you pause — go back to that slide in this guide and re-read it."],
say="Thirty words, all earned, all one-liners. Screenshot."),

dict(n=38, title="Cheat sheet 3 — the knob table",
mean=["The practical-round drill table: 'change this → that happens'. The competition's practical questions usually ask exactly this — predict the effect of a change BEFORE running the code.",
"Practice idea for a team session: cover the right column, read a knob out loud, let everyone predict, then reveal."],
say="Cover the right column and quiz each other — this is exactly the practical-round skill."),

dict(n=39, title="Closing — it was never magic",
mean=["The closing message: everything in the deck was multiply, add, and practice. Everything ran on an ordinary laptop, every number was real, and the notebooks to re-run them are in the team repo.",
"End on the empowering note: nothing today was beyond them — they computed the core operation by hand within the first ten minutes."],
say="It was never magic. It was multiply, add, and practice. Go win."),
]
