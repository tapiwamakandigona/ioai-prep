# Core dependencies for running the Jupyter notebooks.
# CPU-only PyTorch — use https://download.pytorch.org/whl/cpu index for smaller install,
# or drop --index-url for GPU support.
torch>=2.0
torchvision>=0.15
matplotlib>=3.7
scikit-learn>=1.3
transformers>=4.30        # needed for CLIP in notebook 2
Pillow>=9.0
jupyter>=1.0

# Build tools (only needed to regenerate decks/notebooks, not to run them)
# python-pptx>=0.6.21
# nbformat>=5.9
# nbclient>=0.8
# weasyprint>=60
