# zwe101 live test + Day-2 wildcard validation (2026-08-02, cram night)

> Third live-test round on `chat.ioai2026.kz` (Gemma 4 E4B), this time on the **zwe101**
> account (per Tapiwa 2026-08-02: "zwe101 is mine now" — reassignment is his statement,
> not independently confirmed; ⚠️ update `plan/01_lock_in.md`'s "zwe104 is YOUR account"
> line and confirm with team leaders that the reassignment is registered officially).
> 12 messages spent, **48 left in the hour for tonight's self-drill**. Every returned
> cell was executed locally against synthetic data matching the exact spec (VERIFIED =
> ran + scored, not read).

## Account facts (VERIFIED 2026-08-02)
- zwe101 login works; track **GAITE**, model **Gemma 4 E4B**, 2,000-token replies,
  10-msg memory, 2,000-char messages, **60 msg/h** — identical limits to zwe104.
- `/instructions` on zwe101 carries its own **Eastern contest login** (separate creds,
  `contest.app/contests/97603`) — same wiring as documented in `research/11`.
- The account had ~10 pre-existing chats (not read).

## What was tested: the 6 Day-2 wildcard recipes (doc 15) as live P3 payloads

| Family | First reply | Local verification | Recovery needed |
|---|---|---|---|
| 1. Text classification (char-ngram TF-IDF + LogReg) | ✅ clean | **acc 1.00** on separable synthetic | none |
| 2. Model repair (find + copy corrupted module) | ⚠️ runs-but-wrong | found the right module, **but `load_state_dict(strict=False)` silently overwrote ALL modules** — spec said copy only the corrupted one | spot it by reading: any `strict=False` on a full state_dict is a red flag |
| 3. Segmentation (per-pixel LogReg) | ⚠️ runs-but-wrong | **silent axis bug**: `reshape(N*H*W, C)` on channel-first data scrambles features → IoU **0.354** vs 0.999 correct | **P5 with a suspicion hint** ("I suspect the reshape mixes up the channel axis; X is channels-FIRST") → correct `transpose(0,2,3,1)` fix, IoU **0.9992** ✅ |
| 4. Time series (lags + rolling + Ridge, time-ordered split) | ❌ crash | `KeyError 'y'` — dropped `y` from a test frame that never had it | **P4 with traceback** → fixed first try, runs ✅ |
| 5. Feature engineering (fixed model, matrix samples) | ❌ crash | zero-dim concat bug | **P4 failed too** (rambling, self-contradicting, still crashed — "two failed fixes" case). **Fresh chat + fully prescriptive spec** (exact vector lengths, exact order, "no comments") → perfect 15-line function, features (N,123), **val/test acc 1.00** ✅ |
| 6. Multimodal late fusion (hstack + LogReg) | ❌ crash | `multi_class` kwarg **removed in current sklearn** — deprecated-API failure mode strikes sklearn too, not just pandas | **P4 with traceback** → fixed first try, **macro F1 1.00** ✅ |

**Bottom line: all 6 wildcard recipes are now live-verified as P3 payloads** — doc 15
promoted from ASSUMED to VERIFIED. Every failure was recovered by an existing pack rule
(P4 loop, P5 with suspicion hint, fresh-chat-after-two-failures). The pack's discipline
is the product; first replies are ~50/50.

## 🚨 New failure mode #12: P7 false PASS on the test-data trap

Fed P7 a cell that **visibly rotates test images at predict time** (the exact Eastern-round
planted trap). Reply:

> `(1) PASS — Xt = np.stack([rotate(np.array(Image.open(p))..., np.random.uniform(-30,30), ...)])`

It **quoted the offending line and called it PASS**, while correctly flagging the banned
`scipy.ndimage` import on point 3. Conclusion:

- **P7 is reliable for mechanical checks** (banned imports, output file writing) but
  **NOT for the augmentation/test-modification trap.**
- The "never modify test data" check is a HUMAN check: before every submit, personally
  grep the code for anything that touches test inputs (rotate/noise/augment/random
  anywhere near `test`). 30 seconds, non-delegable.
- Added to doc 12's P7 section.

## Confirmed E4B behavior stats (this round)
- 6/12 replies needed no recovery; 4 fixed by one P4/P5; 1 needed the fresh-chat rule;
  1 (P7) produced a dangerous false negative.
- "max N lines" still soft; stray `python` fence line still present in the copy buffer;
  comments creep in unless you say "no comments".
- Message budget for a full archetype test incl. recoveries: ~2 msgs. A 6-hour contest
  day at 60/h is **not** the binding constraint — discipline is.
